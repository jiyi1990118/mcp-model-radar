# 添加到 ~/.claude.json

在 `~/.claude.json` 文件中，找到或添加 `mcpServers` 部分：

```json
{
  "mcpServers": {
    "ai-model-intelligence": {
      "command": "node",
      "args": [
        "/Users/jary/Desktop/tempDocs/modelRadar/dist/server.js"
      ],
      "env": {
        "DB_TYPE": "sqlite",
        "SQLITE_DB_PATH": "/Users/jary/Desktop/tempDocs/modelRadar/modelradar.db"
      }
    }
  }
}
```

## 手动编辑步骤

1. 打开配置文件：
```bash
code ~/.claude.json
# 或
vim ~/.claude.json
```

2. 如果文件中已有 `mcpServers`，在其中添加 `ai-model-intelligence` 配置

3. 如果没有 `mcpServers`，在根级别添加整个 `mcpServers` 对象

4. 保存文件

5. 重新加载：输入 `/mcp` 命令
